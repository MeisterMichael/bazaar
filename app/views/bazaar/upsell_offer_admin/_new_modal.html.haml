#new_upsell_offer_modal.modal.fade
	.modal-dialog
		.modal-content
			.modal-header
				%button.close{ type: :button, 'data-dismiss' => 'modal', 'aria-hidden' => 'true' }
					&times;
				%h4.modal-title
					New Upsell Offer
			= form_for BazaarCore::UpsellOffer.new( args ), url: bazaar.upsell_offer_admin_index_path do |f|
				= f.hidden_field :src_offer_id
				= f.hidden_field :src_product_id
				= f.hidden_field :status, value: 'active'
				.modal-body

					- if f.object.src_offer
						.form-group
							%label Source Offer
							%div=f.object.src_offer.title
					- if f.object.src_product
						.form-group
							%label Source Product
							%div=f.object.src_product.title
					.form-group
						= f.label :upsell_type
						= f.collection_select :upsell_type, BazaarCore::UpsellOffer.upsell_types, :first, humanize_first, {}, class: 'form-control'
					.form-group
						= f.label :full_price_offer_id, "Full Price Offer"
						%i.fa.fa-question-circle{ data: { toggle: 'tooltip' }, title: 'The full price equivalent offer, which can be used to calculate the amount of saving the upsell offer provides.' }
						= f.collection_select :full_price_offer_id, BazaarCore::Offer.active.order( title: :asc ), :id, :title, {}, class: 'form-control'
					.form-group
						= f.label :offer_id, "Upsell Offer"
						%i.fa.fa-question-circle{ data: { toggle: 'tooltip' }, title: 'The offer you want to be made as an upsell.' }
						= f.collection_select :offer_id, BazaarCore::Offer.active.order( title: :asc ), :id, :title, {}, class: 'form-control'
				.modal-footer
					%button.btn.btn-default{ type: :button, 'data-dismiss' => 'modal' }
						Close
					= f.submit 'Create', class: 'btn btn-primary'
