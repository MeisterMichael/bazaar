- if states.present?
	= f.collection_select :geo_state_id, states, :id, :name, { prompt: true }, { id: 'order_billing_address_state', class: "form-control" }
- else
	= f.text_field :state, class: "form-control"
